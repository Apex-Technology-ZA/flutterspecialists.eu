---
/**
 * Deprecated tag pagination route from CMS era.
 * Tag filtering is now handled on /articles/ via query params (?tag=&page=).
 * Returning no paths to exclude this route from the build.
 */
export async function getStaticPaths() { return []; }
---