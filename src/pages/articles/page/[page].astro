---
/**
 * Deprecated dynamic pagination route from the CMS experiment.
 * Pagination is now handled via query params on /articles/ (?page=).
 * Returning no paths excludes this route from the build.
 */
export async function getStaticPaths() {
  return [];
}
---